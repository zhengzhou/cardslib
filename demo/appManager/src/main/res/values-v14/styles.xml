<resources>

    <!--
        Base application theme for API 14+. This theme completely replaces
        AppBaseTheme from BOTH res/values/styles.xml and
        res/values-v11/styles.xml on API 14+ devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Holo.Light">

        <!-- API 14 theme customizations can go here. -->
        <item name="android:actionBarStyle">@style/MyActionBar</item>
        <item name="android:actionBarSplitStyle">@style/MyActionBar</item>
        <item name="android:actionBarTabTextStyle">@style/MyActionBar.TabText</item>
        <item name="android:actionMenuTextColor">@color/green</item>
    </style>
    <!-- ActionBar styles -->
    <style name="MyActionBar" parent="@android:style/Widget.Holo.Light.ActionBar.Solid.Inverse">
        <item name="android:background">@color/white</item>
        <item name="android:backgroundSplit">@color/white</item>
        <item name="android:titleTextStyle">@style/MyActionBar.TitleText</item>
        <item name="android:indeterminateProgressStyle">@style/MyActionBar.IndeterminateProgress</item>
    </style>
    <!-- ActionBar title text -->
    <style name="MyActionBar.TitleText" parent="@android:style/TextAppearance.Holo.Widget.ActionBar.Title">
        <item name="android:textColor">@color/black</item>
    </style>

    <!-- ActionBar tabs text styles -->
    <style name="MyActionBar.TabText" parent="@android:style/Widget.Holo.ActionBar.TabText">
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="MyActionBar.IndeterminateProgress" parent="@android:style/Widget.ProgressBar.Small">
        <item name="android:indeterminateDrawable">@drawable/refresh_rotate</item>
        <item name="android:paddingRight">10dp</item>
        <item name="android:minWidth">32dp</item>
        <item name="android:maxWidth">48dp</item>
        <item name="android:minHeight">32dp</item>
        <item name="android:maxHeight">48dp</item>
        <item name="android:gravity">center</item>
    </style>

</resources>